{
  "name": "yami-warika-dev",
  "private": true,
  "description": "Build tooling for 闇ワリカ — not part of the deployed app itself (see ../index.html).",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "node tests/test.mjs && node tests/test-delete.mjs && node tests/test-finalgacha.mjs && node tests/test-jackpot.mjs"
  },
  "dependencies": {
    "qrcode-generator": "^2.0.4"
  },
  "devDependencies": {
    "playwright": "^1.56.0"
  }
}
